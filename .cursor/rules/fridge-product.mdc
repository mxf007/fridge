---
description: 今晚冰箱产品边界。文档优先，禁止自动落格和效果图多余物。
alwaysApply: true
---

# 今晚冰箱 · 产品锁

源文件：`docs/今晚冰箱_玩法文档.md`、`docs/今晚冰箱_UI设计.md`。冲突时以这两份为准，不要以 `docs/ui_refs/` 效果图为准。

- 微信竖屏 **720×1280**，Cocos **3.8.3**，工程 `F:\fridge`。不要改 `blue_bean`，不要复用船/子弹/波次。
- **先选目标格（不计步），再点食材落子（计步）。** 禁止点食材后自动找格、自动进柜台。
- 不要拖拽、摇杆、倒计时、金币条、体力、底导航、BGM。
- 效果图里的豆浆/豆腐/鸡蛋、Undo 英文、底栏图标是生成多余物，不要做进关卡或 UI。
- 第 1–9 关柜台不渲染、不可选、不可收（`bufferEnabled = id >= 10`）。
- 第 4、7 关「不换格则青菜弹回」不过，禁止铺 12–30 关 JSON。第 24、27 关表内件数有误，改表前不要按表填。
- 分享按钮文案是「让好友也收这一层」，不是「请人看一眼」。
- 一次只做 `docs/开工步骤.md` 里的一步。用户说「只做步骤 N」时禁止做 N+1。第 1/4/7 关不过，禁止铺 12–30 关。
- 14 天原型：先 `GameScene` + 纯数据 `BoardState` + 第 1 关。主页、音效、广告 SDK 后做。
