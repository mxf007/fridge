---
description: 对局 UI 用现有切图和文档坐标，禁止抄效果图布局。
globs: assets/**/*.{ts,prefab,scene}
alwaysApply: false
---

# 对局 UI

小图只用 `assets/ui/` 现有去背图：`food_*` 进冰箱格，`bag_*` 进购物袋列（对齐等宽叠放）。选中描边、提示虚线、失败闪红 **代码画**。

```
y 96   关卡号（点出暂停）     [撤销] [提示]
y 180  冰箱槽 2–5（横向；cap4 缩放 1.15，cap2 缩放 0.85）
y 560  购物袋列，栈顶 100% 亮，下层 60% + scale 0.92
y 980  柜台（仅 id>=10；1–9 留白）
```

- 热区 ≥ 88。全屏同时只一个 Coral 主按钮。字用系统圆体，色板见 UI 文档。
- 不要从 `docs/ui_refs/` 裁新图。缺图：先色块占位或一件一图 + 品红去背，不要整屏裁。
- 1–6 关不显示提示钮。1–9 关不要画 `buffer_board` / `buffer_slot`。
- 动效按 UI §7：飞入 220ms、封格 280ms、弹回 80ms、选中切换 80ms。不要烟花、金币雨。
